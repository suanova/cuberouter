{{- $f := include "cuberouter.fullname" . }}
{{- $cluster := include "cuberouter.postgresClusterName" . }}
{{- $user := .Values.postgresql.auth.username }}
{{- $db := .Values.postgresql.auth.database }}
apiVersion: postgres-operator.crunchydata.com/v1beta1
kind: PostgresCluster
metadata:
  name: {{ $cluster }}
  labels:
    {{- include "cuberouter.labels" . | nindent 4 }}
spec:
  {{- if .Values.postgresql.image }}
  image: {{ .Values.postgresql.image }}
  {{- end }}
  postgresVersion: {{ .Values.postgresql.postgresVersion }}
  users:
    - name: {{ $user }}
      databases:
        - {{ $db }}
  instances:
    - name: instance1
      replicas: {{ .Values.postgresql.replicas }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    postgres-operator.crunchydata.com/cluster: {{ $cluster }}
                topologyKey: kubernetes.io/hostname
      dataVolumeClaimSpec:
        accessModes:
          {{- toYaml (list "ReadWriteOnce") | nindent 10 }}
        {{- if .Values.postgresql.storageClassName }}
        storageClassName: {{ .Values.postgresql.storageClassName }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.postgresql.storage }}
      resources:
        {{- toYaml .Values.postgresql.resources | nindent 8 }}
  {{- if .Values.postgresql.backups.enabled }}
  backups:
    pgbackrest:
      repoHost:
        dedicated: {}
      repos:
        - name: repo1
          volume:
            volumeClaimSpec:
              accessModes:
                - ReadWriteOnce
              resources:
                requests:
                  storage: {{ .Values.postgresql.backups.storage }}
      schedule:
        full: {{ .Values.postgresql.backups.schedule | quote }}
  {{- end }}
  {{- if .Values.postgresql.pgBouncer.enabled }}
  proxy:
    pgBouncer:
      replicas: {{ .Values.postgresql.pgBouncer.replicas }}
  {{- end }}
{{- if .Values.postgresql.initJob.enabled }}
---
{{/*
Post-install hook: waits for the Crunchy cluster, then syncs the app role
password (the operator auto-generates passwords, this chart uses its own)
and grants schema rights. Idempotent; safe to re-run on upgrades.
*/}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ $cluster }}-init
  labels:
    {{- include "cuberouter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
    "helm.sh/hook-weight": "10"
spec:
  backoffLimit: 10
  template:
    metadata:
      labels:
        {{- include "cuberouter.selectorLabels" (dict "root" . "component" "postgresql") | nindent 8 }}
    spec:
      restartPolicy: Never
      containers:
        - name: init
          image: {{ .Values.postgresql.initJob.image | default (include "cuberouter.postgresImage" .) }}
          imagePullPolicy: IfNotPresent
          env:
            - name: PGHOST
              value: {{ $cluster }}-primary
            - name: PGUSER
              value: postgres
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $cluster }}-pguser-postgres
                  key: password
            - name: APP_USER
              value: {{ $user }}
            - name: APP_DB
              value: {{ $db }}
            - name: APP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "cuberouter.secretName" . }}
                  key: POSTGRES_PASSWORD
          command:
            - bash
            - -ec
            - |
              echo "waiting for postgres primary ${PGHOST} ..."
              for _ in $(seq 1 300); do
                if pg_isready -q -h "$PGHOST" -U "$PGUSER"; then
                  break
                fi
                sleep 2
              done
              pg_isready -q -h "$PGHOST" -U "$PGUSER" || { echo "postgres did not become ready in time"; exit 1; }

              psql_admin() { psql -v ON_ERROR_STOP=1 -h "$PGHOST" -U "$PGUSER" "$@"; }

              if [ -z "$(psql_admin -tA -c "SELECT 1 FROM pg_roles WHERE rolname = '$APP_USER'")" ]; then
                echo "creating role $APP_USER"
                psql_admin -c "CREATE ROLE \"$APP_USER\" LOGIN"
              fi
              echo "syncing password for role $APP_USER"
              psql_admin -c "ALTER ROLE \"$APP_USER\" WITH PASSWORD '$APP_PASSWORD'"

              if [ -z "$(psql_admin -tA -c "SELECT 1 FROM pg_database WHERE datname = '$APP_DB'")" ]; then
                echo "creating database $APP_DB"
                psql_admin -c "CREATE DATABASE \"$APP_DB\" OWNER \"$APP_USER\""
              fi

              psql_admin -d "$APP_DB" -c "GRANT ALL ON SCHEMA public TO \"$APP_USER\""
              psql_admin -d "$APP_DB" -c "ALTER SCHEMA public OWNER TO \"$APP_USER\""

              echo "postgres initialization complete"
{{- end }}
